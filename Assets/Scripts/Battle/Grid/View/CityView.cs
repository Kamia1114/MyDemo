using Battle.Grid;

namespace Battle.Grid
{
    /// <summary>
    /// 城市地块视图，可以购买、升级的地块类型
    /// </summary>
    public class CityView : GridItem
    {
        // Start is called before the first frame update
        void Start()
        {

        }

        // Update is called once per frame
        void Update()
        {

        }

    }
}
